# Environment variables for production
NODE_ENV=production
PORT=3001

# Optional: Set a different data directory
# DATA_DIR=/var/lib/botc/data

# Optional: Set CORS origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000,http://your-domain.com
